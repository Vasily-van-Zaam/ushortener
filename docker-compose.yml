version: '3.6'
services:
  postgres:
    image: postgres:12
    restart: always
    volumes:
      - ${PWD}/_data/postgresql:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: postgrespassword
    ports:
      - "5439:5432"